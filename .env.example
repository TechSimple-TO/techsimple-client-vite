# Example environment for Vite + EmailJS
#
# Copy this file to `.env.local` and fill in the values
# from your EmailJS dashboard. `.env.local` is git-ignored.
#
# Notes:
# - Vite only exposes variables prefixed with `VITE_`.
# - Restart the dev server after adding/updating variables.
# - The contact form sends fields: name, email, title, message.

VITE_EMAILJS_SERVICE_ID=service_xxx
VITE_EMAILJS_TEMPLATE_ID=template_xxx
VITE_EMAILJS_PUBLIC_KEY=public_xxx
